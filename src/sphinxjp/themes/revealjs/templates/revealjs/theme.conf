[theme]
inherit = basic
stylesheet = revealjs.css
pygments_style = friendly

[options]
# Set the lang attribute of the html tag. Defaults to "ja"
lang = ja

# The "normal" size of the presentation, aspect ratio will be preserved
# when the presentation is scaled to fit different resolutions
width = 960
height = 700

# Factor of the display size that should remain empty around the content
margin = 0.1

# Bounds for smallest/largest possible scale to apply to content
min_scale = 0.2
max_scale = 1.0

# Display controls in the bottom right corner
controls = true

# Determines where controls appear, "edges" or "bottom-right" (new)
controls_layout = bottom-right

# Visibility rule for backwards navigation arrows; "faded", "hidden" or "visible" (new)
controls_back_arrows = faded

# Display a presentation progress bar
progress = true

# Set default timing of 2 minutes per slide. (new)
default_timing = 120

# Display the page number of the current slide (new)
slide_number = false

# Push each slide change to the browser history
history = true

# Enable keyboard shortcuts for navigation
keyboard = true

# Enable the slide overview mode
overview = true

# Vertical centring of slides
center = true

# Enables touch navigation on devices with touch input
touch = true

# Loop the presentation
loop = false

# Change the presentation direction to be RTL
rtl = false

# Randomizes the order of slides each time the presentation loads
shuffle = false

# Turns fragments on and off globally
fragments = true

# Flags if the presentation is running in an embedded mode,
# i.e. contained within a limited portion of the screen
embedded = false

# Flags if we should show a help overlay when the questionmark
# key is pressed
help = true

# Flags if speaker notes should be visible to all viewers
show_notes = false

# Global override for autoplaying embedded media (video/audio/iframe)
# - null: Media will only autoplay if data-autoplay is present
# - true: All media will autoplay, regardless of individual setting
# - false: No media will autoplay, regardless of individual setting
auto_play_media = null


# Number of milliseconds between automatically proceeding to the
# next slide, disabled when set to 0, this value can be overwritten
# by using a data-autoslide attribute on your slides
auto_slide = 0

# Stop auto-sliding after user input
auto_slide_stoppable = true

# Use this method for navigation when auto-sliding. (Not support)
# auto_slide_method = Reveal.navigateNext

# Enable slide navigation via mouse wheel
mouse_wheel = false

# Hides the address bar on mobile devices
hide_address_bar = true

# Opens links in an iframe preview overlay
# Add `data-preview-link` and `data-preview-link="false"` to customise each link
# individually
preview_links = false

# Theme (black/white/league/beige/sky/night/serif/simple/solarized)
theme = black

# Transition style (default/none/fade/slide/convex/concave/zoom)
transition = default

# Transition speed (default/fast/slow)
transition_speed = default

# Transition style for full page slide backgrounds (default/none/fade/slide/convex/concave/zoom)
background_transition = default

# Number of slides away from the current that are visible
view_distance = 3

# Parallax background image
# CSS syntax, e.g. "a.jpg"
parallax_background_image =

# Parallax background size
# CSS syntax, e.g. "3000px 2000px"
parallax_background_size =

# Number of pixels to move the parallax background per slide
# - Calculated automatically unless specified
# - Set to 0 to disable movement along an axis
parallax_background_horizontal =
parallax_background_hertical = 

# The display mode that will be used to show slides
display = block

# Focuses body when page changes visiblity to ensure keyboard shortcuts work
focus_body_on_page_visibility_change = true

# Enable plguin javascript for reveal.js
plugin_list =

# config for Multiplexing
multiplex =

# config for Leap motion
leap =

# config for MathJax
math =

# loading custom js after RevealJs.initialize.
customjs =
